extends layout

block content
	h1.align-center Welcome to #{title}
	br
	include _newList
	
	- if (lists)
		ul.bulletless
			- each list in lists
				li
					a(href='/lists/' + list._id, class='btn')= list.name